import { NextPage } from 'next';

/**
 * The page component to render at "/".
 *
 * @returns {NextPage} The home page component.
 */
const Home: NextPage = () => {
    return <></>;
};

export default Home;
